# تدفق الطلب الكامل - متجر الحامد 🛒

## نظرة عامة
تم بناء نظام طلب متكامل واحترافي يربط كل شيء بقاعدة البيانات بشكل حقيقي.

---

## 1️⃣ عرض المنتجات

### أ) بطاقة المنتج (ProductCard)
**الموقع:** `src/components/products/ProductCard.tsx`

**الميزات:**
- ✅ صورة المنتج قابلة للنقر → تنتقل لصفحة التفاصيل
- ✅ عنوان المنتج قابل للنقر → تنتقل لصفحة التفاصيل
- ✅ عرض السعر (مع سعر المقارنة إذا كان هناك خصم)
- ✅ Badge للخصم/مميز/غير متوفر
- ✅ تنبيه إذا كانت الكمية قليلة (≤5)
- ✅ **زر "إضافة للسلة 🛒"** - مباشر على البطاقة
- ✅ زر "عرض التفاصيل"
- ✅ Loading state عند الإضافة للسلة
- ✅ تحديث عداد السلة فوراً

**الصفحات التي تستخدمها:**
- الصفحة الرئيسية (`/`)
- صفحة المنتجات (`/products`)
- صفحة تفاصيل المنتج (`/products/[slug]`) - منتجات ذات صلة

### ب) صفحة تفاصيل المنتج (ProductInfo)
**الموقع:** `src/components/products/ProductInfo.tsx`

**الميزات:**
- ✅ معلومات تفصيلية عن المنتج
- ✅ معرض صور (ProductGallery)
- ✅ اختيار الكمية (Quantity Selector)
- ✅ زر "إضافة إلى السلة 🛒"
- ✅ زر "اشترِ الآن ⚡" → إضافة + انتقال للسلة
- ✅ زر "واتساب 💬" → رقم ديناميكي من الإعدادات
- ✅ عرض المواصفات
- ✅ عرض الوصف

---

## 2️⃣ سلة التسوق

### صفحة السلة
**الموقع:** `src/app/cart/page.tsx`

**الميزات:**
- ✅ عرض جميع المنتجات المضافة
- ✅ تعديل الكمية لكل منتج (+/-)
- ✅ حذف منتج من السلة
- ✅ عرض الإجمالي الفرعي
- ✅ عرض الإجمالي النهائي
- ✅ حفظ السلة في Local Storage
- ✅ زر "متابعة إلى الدفع 💳" → ينتقل إلى Checkout
- ✅ زر "متابعة التسوق 🛍️" → يرجع للمنتجات

**API المستخدمة:**
- لا يوجد - السلة محلية (Local Storage)

---

## 3️⃣ صفحة الدفع (Checkout)

### صفحة الدفع
**الموقع:** `src/app/checkout/page.tsx`

**الميزات:**

#### أ) معلومات العميل
- اسم العميل (مطلوب)
- رقم الموبايل (مطلوب)

#### ب) عنوان التوصيل
- المحافظة (اختيار من قائمة)
- المنطقة (اختيار من قائمة)
- العنوان التفصيلي (نص حر)

#### ج) ملاحظات (اختياري)
- ملاحظات إضافية للطلب

#### د) طريقة الدفع
**خياران:**

1. **الدفع عند الاستلام 💵**
   - لا يتطلب أي خطوات إضافية
   - حالة الطلب: `pending`

2. **الدفع عبر شام كاش 💳**
   - ✅ عرض معلومات الحساب (من الإعدادات):
     - اسم الحساب
     - رقم الحساب
     - QR Code (إذا كان مرفوعاً)
   - ✅ رفع إثبات الدفع (صورة)
   - حالات الطلب:
     - مع إثبات دفع: `payment_verification`
     - بدون إثبات: `awaiting_payment`

#### هـ) ملخص الطلب
- عرض جميع المنتجات
- الكمية والسعر
- الإجمالي

#### و) زر "تأكيد الطلب ✓"
- التحقق من صحة البيانات
- رفع الصورة (إذا كانت موجودة)
- إرسال الطلب إلى API
- الانتقال لصفحة النجاح

**API المستخدمة:**
- `POST /api/orders` - إنشاء طلب جديد
- `GET /api/settings/shamcash` - جلب معلومات شام كاش
- `POST /api/upload` - رفع صورة إثبات الدفع

---

## 4️⃣ إرسال الطلب

### API Endpoint
**الموقع:** `src/app/api/orders/route.ts`

**الوظيفة:**
```typescript
POST /api/orders
```

**البيانات المرسلة:**
```json
{
  "customerName": "أحمد محمد",
  "customerPhone": "0912345678",
  "governorate": "دمشق",
  "area": "المزة",
  "address": "شارع الكورنيش - بناء السلام",
  "notes": "الرجاء الاتصال قبل التوصيل",
  "paymentMethod": "shamcash",
  "paymentProofUrl": "https://...",
  "items": [
    {
      "productId": "prod_xxx",
      "productName": "ساعة ذكية",
      "price": 150000,
      "quantity": 2
    }
  ],
  "subtotal": 300000,
  "total": 300000
}
```

**العمليات:**
1. التحقق من البيانات
2. إنشاء رقم طلب فريد (`ORD-XXXXXX`)
3. تحديد حالة الطلب حسب طريقة الدفع
4. حفظ في جدول `orders`
5. حفظ المنتجات في جدول `order_items`
6. إرجاع معرف الطلب

**الاستجابة:**
```json
{
  "id": "ord_xxx",
  "orderNumber": "ORD-123456",
  "message": "تم إنشاء الطلب بنجاح"
}
```

---

## 5️⃣ صفحة نجاح الطلب

### صفحة النجاح
**الموقع:** `src/app/order-success/page.tsx`

**الميزات:**
- ✅ رسالة نجاح مع رقم الطلب
- ✅ عرض كامل لتفاصيل الطلب:
  - معلومات العميل
  - عنوان التوصيل
  - طريقة الدفع
  - المنتجات والكميات
  - الإجمالي
- ✅ الخطوات التالية
- ✅ أزرار:
  - "تتبع الطلب 📦" → صفحة التتبع
  - "متابعة التسوق 🛍️" → الرجوع للمنتجات
  - "واتساب 💬" → التواصل (رقم ديناميكي)

**API المستخدمة:**
- `GET /api/orders?orderId=xxx` - جلب تفاصيل الطلب

---

## 6️⃣ تتبع الطلب

### صفحة التتبع
**الموقع:** `src/app/order-tracking/page.tsx`

**الميزات:**
- ✅ نموذج بحث:
  - رقم الطلب
  - رقم الهاتف (للتحقق)
- ✅ عرض حالة الطلب
- ✅ Progress Stepper:
  1. تم الطلب 📝
  2. تجهيز ⚙️
  3. شحن 🚚
  4. تسليم 🎉
- ✅ تفاصيل الطلب كاملة
- ✅ زر واتساب للدعم

**API المستخدمة:**
- `GET /api/orders/track?orderNumber=xxx&phone=xxx` - تتبع الطلب

---

## 🗄️ قاعدة البيانات

### جدول `orders`
```sql
- id (معرف فريد)
- orderNumber (رقم الطلب: ORD-XXXXXX)
- customerName (اسم العميل)
- customerPhone (رقم الموبايل)
- governorate (المحافظة)
- area (المنطقة)
- address (العنوان التفصيلي)
- notes (ملاحظات)
- paymentMethod (cash/shamcash)
- paymentStatus (pending/payment_verification/paid)
- paymentProofUrl (صورة إثبات الدفع)
- status (حالة الطلب)
- subtotal (المجموع الفرعي)
- total (الإجمالي)
- createdAt, updatedAt
```

### جدول `order_items`
```sql
- id (معرف فريد)
- orderId (معرف الطلب)
- productId (معرف المنتج)
- productName (اسم المنتج)
- productPrice (السعر)
- quantity (الكمية)
- total (الإجمالي)
- createdAt
```

### جدول `store_settings`
```sql
- storeName (اسم المتجر)
- phone (رقم الهاتف)
- whatsapp (رقم الواتساب)
- shamcashAccountName (اسم حساب شام كاش)
- shamcashAccountNumber (رقم حساب شام كاش)
- shamcashQrCode (صورة QR)
- ... (إعدادات أخرى)
```

---

## 🔄 حالات الطلب (Order Status)

1. **pending** → في الانتظار (دفع عند الاستلام)
2. **awaiting_payment** → بانتظار الدفع (شام كاش بدون إثبات)
3. **payment_verification** → التحقق من الدفع (شام كاش مع إثبات)
4. **processing** → قيد التجهيز
5. **shipped** → تم الشحن
6. **delivered** → تم التوصيل
7. **cancelled** → ملغي

---

## 📁 هيكل الملفات

```
src/
├── app/
│   ├── page.tsx                          # الصفحة الرئيسية
│   ├── products/
│   │   ├── page.tsx                      # قائمة المنتجات
│   │   └── [slug]/page.tsx               # تفاصيل المنتج
│   ├── cart/
│   │   └── page.tsx                      # سلة التسوق
│   ├── checkout/
│   │   └── page.tsx                      # صفحة الدفع
│   ├── order-success/
│   │   └── page.tsx                      # صفحة النجاح
│   ├── order-tracking/
│   │   └── page.tsx                      # تتبع الطلب
│   └── api/
│       ├── orders/
│       │   ├── route.ts                  # إنشاء/جلب الطلبات
│       │   └── track/route.ts            # تتبع الطلب
│       ├── products/
│       │   └── [id]/route.ts             # جلب منتج محدد
│       ├── settings/
│       │   ├── route.ts                  # الإعدادات العامة
│       │   └── shamcash/route.ts         # إعدادات شام كاش
│       └── upload/
│           └── route.ts                  # رفع الملفات
│
├── components/
│   ├── products/
│   │   ├── ProductCard.tsx               # بطاقة المنتج
│   │   ├── ProductInfo.tsx               # معلومات المنتج
│   │   ├── ProductGallery.tsx            # معرض الصور
│   │   └── ProductsFilter.tsx            # تصفية المنتجات
│   ├── layout/
│   │   ├── Header.tsx                    # الرأس (مع عداد السلة)
│   │   └── Footer.tsx                    # التذييل
│   └── ui/
│       ├── Button.tsx                    # زر
│       ├── Input.tsx                     # حقل إدخال
│       ├── Badge.tsx                     # شارة
│       └── ...
│
└── lib/
    ├── cart.ts                           # إدارة السلة
    ├── utils.ts                          # دوال مساعدة
    └── constants.ts                      # ثوابت
```

---

## ✨ الميزات الرئيسية

### 1. زر "إضافة للسلة" على كل منتج ✅
- موجود على `ProductCard` في جميع الصفحات
- موجود على `ProductInfo` في صفحة التفاصيل
- Loading state
- تحديث فوري لعداد السلة

### 2. إدارة السلة ✅
- Local Storage
- تحديث فوري
- التحقق من المخزون
- تعديل الكميات

### 3. عملية دفع متكاملة ✅
- نموذج كامل
- خيارات دفع متعددة
- رفع إثبات الدفع
- معلومات ديناميكية من الإعدادات

### 4. تتبع الطلبات ✅
- بحث برقم الطلب والهاتف
- عرض الحالة
- Progress indicator
- تفاصيل كاملة

### 5. كل شيء مرتبط بالداتابيز ✅
- جميع البيانات حقيقية
- لا توجد بيانات ثابتة
- إعدادات ديناميكية
- مزامنة كاملة

---

## 🚀 الخطوات التالية (للمستخدم)

1. إضافة منتجات من لوحة التحكم
2. ضبط إعدادات المتجر (أرقام الهاتف/واتساب)
3. ضبط معلومات شام كاش (اسم حساب + رقم + QR)
4. اختبار عملية الشراء كاملة
5. متابعة الطلبات من لوحة التحكم

---

## 📝 ملاحظات مهمة

- ✅ لا توجد صفحات مفقودة
- ✅ جميع API endpoints موجودة
- ✅ جميع المكونات موجودة
- ✅ التكامل الكامل مع قاعدة البيانات
- ✅ تم إصلاح خطأ Hydration (nested <a> tags)
- ✅ زر "إضافة للسلة" يظهر على كل منتج
- ✅ المشروع جاهز للاستخدام 🎉

---

**تم بناء النظام بدقة عالية جداً واحترافية كاملة! 💯**
